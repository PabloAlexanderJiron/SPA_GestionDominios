name: Deplegar en desarrollo

on:
  push:  
    branches: [dev]

jobs:
  CI-CD:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🐧 El job esta corriendo en ${{ runner.os }}!"
      - name: Clonar reporsitorio
        uses: actions/checkout@v4
      - run: echo "💡 El ${{ github.repository }} fue clonado"