name: Deplegar en desarrollo

on:
  push:  
    branches: [dev]

jobs:
  CI-CD:
    runs-on: vps
    steps:
      - run: echo "🐧 El job esta corriendo en ${{ runner.os }}!"
      # - name: Clonar reporsitorio
      #   uses: actions/checkout@v4
      # - run: docker build -f Dockerfile.dev -t spa_gestion_dominios_dev:latest .